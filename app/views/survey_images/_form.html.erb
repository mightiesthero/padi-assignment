<%= form_with(model: survey_image, local: true) do |form| %>
  <% if survey_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey_image.errors.count, "error") %> prohibited this survey_image from being saved:</h2>

      <ul>
      <% survey_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :img %>
    <%= form.text_field :img %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :survey_site_id %>
    <%= form.text_field :survey_site_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
